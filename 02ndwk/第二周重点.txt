1.条件语句
 if/else结构：
    单分支、双分支、多分支条件判断
    嵌套if语句的最佳实践
 switch语句：
    case标签的使用
    break关键字的作用
    default分支的处理
 三元运算符：
    condition？expr1：expr2简洁表达式
 逻辑运算符：
    短路求值特性（&&和||）
    德摩根定律应用
 条件编译
    #ifdef、#ifndef、#endif预处理指令
2.循环结构
 循环类型：
    for循环（确定次数迭代）
    while循环（条件优先）
    do-while循环（至少执行一次）
 循环控制：
    break语句（立即退出循环）
    continue（跳过当前迭代）
 嵌套循环：
    循环内部的循环结构
    循环变量作用域管理
 范围for循环（C++11）：
    for (auto element:container)语法
 循环优化：
    循环展开（loop unrolling）
    减少循环内部计算
3.数组处理
 数组基础：
    一维/多维数组声明与初始化
    数组内存布局（连续内存空间）
 数组操作：
    元素访问与修改
    数组边界检查
    数组作为函数参数
 数组与指针
    数组名作为指针常量
    指针算术运算
 标准库替代
    std::array（固定大小数组）
    std::vector（动态数组）
 数组算法
    查找、排序、遍历优化